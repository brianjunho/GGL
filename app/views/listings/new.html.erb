<h1>新的任务</h1>

<%= render 'form', data: { no_turbolink: true } %>

<% if current_user.try(:admin?) || current_user.try(:editor) %>
	<%= link_to 'Back', listings_path, class: "btn btn-link" %>
<% else %>
	<%= link_to 'Back', root_url, class: "btn btn-link" %>
<% end %>
