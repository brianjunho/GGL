<%= form_for(@listing) do |f| %>
  <% if @listing.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:</h4>

      <ul>
      <% @listing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :prompt %><br>
    <p class="help-block">What is the prompt for your essay?</p>
    <%= f.text_area :prompt, placeholder: "Enter prompt", class: "form-control" %>
  </div> 
  <div class="form-group">
    <%= f.label :application %><br>
    <%= f.text_area :application, rows: 10, placeholder: "Paste application here", class: "form-control" %>
  </div>
    
  <div class="form-group">
      <p><b>Select Option</b></p>
      <p class="row">
      <%= f.radio_button :option, '0' %>
      <%= f.label :option, 'Basic Proofread', :value => 'Basic Proofread' %><br>
      <%= f.radio_button :option, '1' %>
      <%= f.label :option, 'Standard (Comprehensive Proofread)', :value => 'Standard (Comprehensive Proofread)' %>
    </p>
    
  </div>
  <div class="form-group">
    <%= f.label :comments %><br>
    <%= f.text_area :comments, rows: 3, placeholder: "Any other comments to help your editor. Leave blank if not required", class: "form-control" %>
  </div>

  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>

 
<% end %>
