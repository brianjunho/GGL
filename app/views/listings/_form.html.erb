<%= form_for(@listing) do |f| %>
  <% if @listing.errors.any? %>
    <div id="error_explanation" class="alert alert-danger alert-dismissable">
      <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
      <h4><%= pluralize(@listing.errors.count, "error") %> prohibited this listing from being saved:</h4>

      <ul>
      <% @listing.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <b><p>What is the essay type?</p></b>
    <p class="help-block">(ie. Personal Statement, Resume, Sample Writing, etc)</p>
    <%= f.text_field :name, placeholder: "Enter type of document", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :prompt %><br>
    <p class="help-block">What is the essay topic?</p>
    <%= f.text_area :prompt, placeholder: "Enter prompt", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.label :application %><br>
    <%= f.text_area :application, placeholder: "Paste application here", class: "form-control" %>
  </div>
  <div class="form-group">
    <b>Payment </b><%= f.label :option %><br>
    <p class="help-block">0. Basic Proofread <br>1. With Comments</p>
    <%= f.text_field :option, placeholder: "Select Pricing Plan", class: "form-control" %>
  </div>
  <div class="form-group">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
